apiVersion: v1
kind: Service
metadata:
  name: hello-k8s-svc
  namespace: project-01
spec:
  type: NodePort
  selector:
    app: hello-k8s
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080
